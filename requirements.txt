# Stack potwierdzony na Pythonie 3.14 (wheele OK). Wersje luzne dla MVP.
python-dotenv
requests

# --- Parser HTML (Faza 4) ---
beautifulsoup4

# --- Embeddings + retrieval (Faza 7-8) ---
# fastembed = lokalne embeddingi ONNX (keyless, bez torcha); e5-large multilingual
fastembed
faiss-cpu
numpy
rank_bm25            # hybryda leksykalna (BM25) do fuzji RRF

# --- LLM + structured output (Faza 11) ---
anthropic
pydantic

# --- UI (Faza 13) ---
streamlit
